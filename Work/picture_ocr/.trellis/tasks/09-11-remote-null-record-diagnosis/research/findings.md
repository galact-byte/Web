# 调查证据与远程保全

## 诊断与历史

输入：用户本地桌面诊断 JSON（2026-09-11T07:30:29.576Z）。不复制客户名称、项目正文或完整诊断到版本库。

- app=0.7.2，desktop；projects=7、summaries=6、images=570、legacy=0。
- damagedRecords 唯一项目 project-1788850797464-1tebd3：reason=invalid-value、valueType=null。
- 对应代码：src/utils/db.ts:155 的 get 成功回调，:166 的异常记录分支。自检仅写摘要、不写异常主文档，也不读取 images。
- 9 月 8 日的 null.id 日志没有主键，不能证明与本次同一条记录，更不能归因为强制关窗、图片过大或某次迁移。
- git 4f33857：0.6.1 升级只建表，普通事务回填时跳过 null/缺 id；是从列表排除，不是从 projects 删除。
- git 5d2134d：0.6.2 按主键修复摘要，无法读取明确报告。
- git ea62b56：0.7.1 修复重复提示。
- git 5fa4da0 / RELEASE-NOTES.md:13：0.7.2 补诊断，不承诺已有记录自动恢复。
- trellis mem extract 01a08ec0-c0c --grep null：上一会话已明确上述边界；此前“恢复全部项目与图片”的表述过度。

## 代码发现

1. StorageSettingsDialog.tsx:159 仅执行 ensureSummariesSynced；errorLog.ts:126 的报告无按异常 ID 的图片统计。570 是全库计数，不能分配给缺失系统。
2. db.ts:666 的 reconcileProjectImages 未被应用其他代码调用；raw 为 null/undefined 时直接返回 missing=[]、orphans=[]，不能据此认为数据一致。
3. db.ts:739 的迁移把 !raw 当作已删除返回 false；db.ts:916 随后无条件加入 completedIds。因此 null 可被误记已完成检查。仅为代码发现，不能证明对方执行过优化或迁移状态已经如此。
4. imageStore.ts:41 的 StoredImage 保留 projectId/imageId/文件名/字节/时间，没有 assetId/itemId/完整系统文档。即使找到图片，也不能据此自动复原资产与检查项归属。
5. db.ts:77 的 projects store 使用 keyPath=id；本次 null 是读取观察值，不能据此断言应用主动 put(null) 或磁盘损坏。根因要在副本上进一步核实。
6. electron/dataLocation.cjs:14、:195 与 electron/main.cjs:299：迁移旧备份保留 7 天，启动就绪约 3 秒后执行到期清理。这是数据目录迁移备份，不是日常自动版本备份。
7. StorageSettingsDialog.tsx:253 显示当前实际目录；:279 显示旧备份目录。dataLocation.cjs:139 返回 current/defaultDir/backup。当前目录可能已迁移，不能让对方只复制 EXE 目录或固定猜测的 C 盘路径。

## 可直接转发的取证步骤

请先不要再次升级、重装、清理、导入覆盖、图片优化或更改存储位置。

### 应用当前仍开着

1. 在“存储设置”截图“当前数据存储目录”；若有“旧数据备份”，同时截图该路径。不要点更改位置、恢复默认或删除备份。
2. 等待保存完成，正常退出程序。若提示尚未保存，选择等待保存完成，不强制结束进程。
3. 在资源管理器按截图路径找到目录，将整个目录复制到另外新建的文件夹（例如其他磁盘的“故障数据保全-日期”），不能只复制 EXE 或几个 .ldb 文件。旧备份目录若仍存在，另存一份。备份目标不得位于原目录内部，禁止剪切和覆盖。
4. 如果出现文件被占用或复制失败，保留现场并记录错误，不跳过文件后称备份完成。需确认程序及相关进程完全退出。

### 应用已经关了

不要为了查看路径重新启动。若此前知道存储位置，直接复制完整目录；若不知道，在资源管理器打开 %APPDATA%，查找本工具相关目录（名称可能为 picture_ocr 或“测评证据采集工具”），只查看其中 data-location.json。文件中的 dataDir 是自定义目录线索、backup.dir 是旧备份线索；migration 表示待执行迁移，应保全其 from/to 且不启动应用。配置可能不存在或目标不可用，不将配置路径当成实际有效库的保证。不确定时先提供文件夹/配置截图，由技术支持定位，勿自行迁移。

### 交付信息

- 当前完整目录副本；仍存在的迁移旧备份另附；已有故障前 .evidence/ZIP 数据包一并保留。
- 已有诊断 JSON，不需要反复导出同一结果。
- 一句话说明：具体少哪个系统/内容，还是仅自检报错。
- 截图记录路径与备份时间；复制完成后比较文件数/总大小，并保留只读原始副本，分析另用副本。
- 数据可能包含测评证据，经双方认可的私密渠道交付，不上传公开仓库或未知第三方服务。

## 后续恢复判断

只在隔离副本读取异常主键、store 结构、关联图片与迁移状态；不直接用正常应用打开唯一备份（启动可能迁移、回填或清理）。完整原文档来自有效旧备份才可评估完整恢复；仅有图片只能评估图片提取，不能声称系统结构恢复；无独立图片也不证明历史内联图片永久不可恢复。底层文件恢复另行评估，不在本次方案内承诺。

## 本轮验证

- npm run verify:summary-repair：44/44。
- npm run verify:error-report：19/19。
- npm run verify:data-location：29/29，含过期清理的临时目录模拟。
- 均为本地纯逻辑、模拟或源码契约验证，不是客户数据库复现或数据恢复证明。
