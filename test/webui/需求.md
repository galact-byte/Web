写一个简化的代码审计工具，核心功能是调用大模型API对代码进行安全审计。我现有的项目在调用API时会中断，所以希望重写一个稳定、轻量的版本

无需Docker，无需审计技能，无需poc库 避免复杂环境。

只需实现API调用，让模型自动审计代码。

可配置API密钥，像使用Claude客户端一样简单。

尽可能简化项目结构和依赖，只需要可以审计和导出报告就行。数据库使用的是mysql

就是写一个webui 装个样子